// Test playWave variant 7: two outputs, wave, two outputs, wave (1, 2, w1, 1, 2, w2)
const WFM_LEN = 1024;
wave w1 = ones(WFM_LEN);
wave w2 = zeros(WFM_LEN);

playWave(1, 2, w1, 1, 2, w2);
wait(100);
setUserReg(0, 7);
