// Test nested conditional statements
var x = 10;
var y = 20;
var z = 30;

if (x < y) {
    if (y < z) {
        setUserReg(0, 1);
    } else {
        setUserReg(0, 2);
    }
} else {
    if (x < z) {
        setUserReg(0, 3);
    } else {
        setUserReg(0, 4);
    }
}

setUserReg(1, x);
