// Analyze setPRNGSeed(seed)

// Set the pseudo-random number generator seed
setPRNGSeed(42);

// Get a value to see if seeding had an effect (not really testable here)
var rand_val = getPRNGValue();
wait(1);
