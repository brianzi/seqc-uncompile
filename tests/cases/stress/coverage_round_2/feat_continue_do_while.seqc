// Coverage Round 2: continue inside do-while — feature combo.

var i = 0;
var hits = 0;
do {
    i = i + 1;
    if (i == 2) { continue; }
    hits = hits + 1;
} while (i < 5);
setUserReg(0, hits);   // 4 (skip i=2)
