// Coverage Round 2: continue inside repeat block — skip side-effect on match.

var k = 0;
var taken = 0;
repeat (10) {
    k = k + 1;
    if (k == 4) { continue; }
    taken = taken + 1;
}
setUserReg(0, k);     // 10
setUserReg(1, taken); // 9
