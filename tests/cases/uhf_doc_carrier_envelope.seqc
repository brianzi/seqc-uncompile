// UHF tutorial: Stabilizing Carrier-Envelope Offset
// Source: docs.zhinst.com/uhf_user_manual/tutorials/tutorial_awg.html
wave w_gauss = gauss(2000, 1000, 200);
while (true) {
  waitDemodOscPhase(4);
  setTrigger(1);
  playWave(1, w_gauss);
  waitWave();
  setTrigger(0);
  wait(100);
}
