const N = 4;
var i;
var count = 0;

setTrigger(1);
wait(10);
setTrigger(0);

for (i = 0; i < N; i++) {
  playZero(32);
  waitWave();
  count = count + 1;
}

repeat(3) {
  playHold(64);
  waitWave();
}

setUserReg(0, count);
